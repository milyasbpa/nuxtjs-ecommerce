import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        quicksand: ["Quicksand", "serif"],
      },
    },
  },
  plugins: [],
};

export default config;
